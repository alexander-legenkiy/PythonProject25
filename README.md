# PythonProject25
## Описание:

Данный проект - это функции на языке программирования Python для обработки различной банковской иформации, а именно: 

1. Файл **masks.py** содержит функции, которые принимают на вход номер банковской карты и номер банковского счёта и возвращают их маски

2. Файл **widget.py** содержит 1) функицю, которая принимает информацию о счетах и картах и обрабатывает их, возвращает в заданном формате с маской из файла masks.py; 2) функцию, которая принимает на вход дату в определённом формате и возвращает дату в заданном формате

3. Файл **processing.py** содержит 1) функцию, которая принимает список словарей и возвращает новый список словарей, исходя из значения ключа state (по умолчанию установлено значение "EXECUTED"; 2) функцию, которая принимает список словарей и возвращает новый список отсортированный по дате, по умолчанию отсортированный по убыванию.

4. Файл **generator.py** содержит 1) функцию, которая принимает на вход список словарей, представляющих транзакции, функция возвращает итератор, который поочередно выдаёт транзакции, где валюта операции соответсвует заданной; 2) генератор, который принимает список словарей с транзакциями и возвращает описание каждой операции по очереди; 3) генератор, который выдаёт номера банковских карт в формате XXXX XXXX XXXX XXXX, где X - цифра номера карты, этот генератор принимает начальное и конечное значение для генерации диапазона номеров и генерирует номера карт в заданном диапазоне от 0000 0000 0000 0001 до 9999 9999 9999 9999.
## Установка:

Клонируйте репозитарий 
```
git clone
[GitHub](https://github.com/alexander-legenkiy/PythonProject25/pull/9)
```

## Использование:

+ В файлe **proccessing.py** в переменную _dictionary_list_ скопируйте свой список словарей содержащий инфомацию об id банковской операции, статутсе операции (исполнена - "EXECUTED" или отменена - "CANCELED"), информацию о дате операции. 

+ Функция _filter_by_state(dictionary_list, state)_ возвращает список тех словарей у которых ключ state соответсвует указанному значению: EXECUTED или CANCELED (по умолчанию в функции применяется значение EXECUTED), т.е. функция _filter_by_state(dictionary_list, state)_ отфильтровывает банковские операции по признаку EXECUTED (Исполненные) или по признаку CANCELLED (Отменённые).    

+ Функция _sort_by_date(dictionary_list: list, reverse)_ возвращает список словарей отсортированный по дате банковской операции, в порядке убывания даты, т.е. сначала отображаются последнии операции, а потом операции произведённые ранее. Для того чтобы изменить порядок сортировки от поздних к последним операциям необходимо выбрать значение переменной reverse равное False.
 
+ Функция _filter_by_currency_ принимает на вход транзакции, в виде списка словарей, и заданную валюту, функция возвращает итератор, который поочерёдно выдаёт транзакции, где валюта операции соответсвует заданной.

+ Функция _transaction_descriptions_ принимает список словарей с транзакциями и возвращает описание каждой операции по очереди.

+ Генератор _card_number_generator_ принимает начальное и конечное значение для генерации диапазона номеров и выдает номера банковкий карт в стандартном формате XXXX XXXX XXXX XXXX.

+ Файлы masks.py, widget.py, processing.py, generator.py проверены линтерами flake8, mypy, isort, black


## Лицензия:
Данный проект распространяется без лицензии

## Тестирование:
+ В файле **test_masks.py** произведено тестирование корректности работы функции _get_mask_card_number_ на предмет корректности маскировки номера карты, в том числе произведена проверка работы функции на количество введённых цифр в номере карты, проверка работы функции на отсутствие букв в номере карты, проверка работы функции, для случая, когда номер карты не был введён. Также произведено тестирование работы функции _get_mask_account_ в части маскировки номера счёта, в том числе произведена проверка работы функции роверка работы функции на количество введённых цифр, проверка работы функции на отсутствие букв в номере счёта, проверка работы функции, для случая, когда номер счёта не был введён.
+ В файле **test_widget.py** произведено тестирование корректности работы функции маскировки номера карты и номера счёта _mask_account_card_, а также тестирование корректности маскировки даты _test_get_date_, в том числе для случаев некорректного ввода даты. Также произведены параметризированные тесты с заданными параметрами номеров карты и номеров счёта, заданными параметрами даты, в том числе для случаев отсутвия введённой даты.
+ В файле **test_processing.py** произведено тестирование корректности работы функции _test_filter_by_state_ с учётом выбора переменной EXECUTED и переменной CANCELED, произведена параметризация тестов для различных возможных статусов переменной state, а также произведено тестирование на ввод пустого списка. Также произведено тестирование функции _test_sort_by_date_ на сортировку списка словарей по датам в порядке убывания и в порядке возрастания, на корректность сортировки при одинаковых датах, на корректность работы функции при нестандартном наборе дат и на ввод пустой даты.
+ В файле **test_generator.py** произведено тестирование корректности работы функции _filter_by_currency_ на предмет вывода транзакций с заданной валютой, а также тестирование для случаев, когда транзакции в заданной валюте отсутствуют, произведено тестирование работы генератора при обработке пустого списка и списка без соответствующих валютных операций. Также произведено тестирование функции _transaction_descriptions_ на корректность вывода описания каждой транзакции, тестирование работы функции с различным количеством входных транзацкций, а также с пустым списком. И произведено тестирование работы генератора _card_number_generator_ на вывод номеров карт, в заданном диапазоне, проверка корректности форматирования номеров карт и корректность обработки крайних значений диапазона.